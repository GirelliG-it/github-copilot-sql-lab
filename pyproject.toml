[project]
name = "sql-open-data-lab"
version = "0.1.0.dev0"
description = "A reproducible analysis of Dutch seaport cargo volumes"
requires-python = ">=3.12,<3.13"
dependencies = [
    "duckdb==1.5.5",
]

[project.optional-dependencies]
test = [
    "pytest==9.1.1",
]
